{
  "id": "osf-registration-protocol",
  "description": "DISCARD twin of `osf-registration-completed`: an OSF registration whose template line is present and whose template is neither a post-completion form nor an Open-Ended Registration carrying the replication stem. What is left is the preregistration vocabulary — `OSF Preregistration`, `AsPredicted`, `OSF-Standard Pre-Data Collection`, `Replication Recipe (Brandt et al., 2013): Pre-Registration`, `Registered Report Protocol Preregistration`, `EGAP` — every one of them a form filed BEFORE data collection, describing a study that has not happened. The record announces an intention, reports no outcome, and gives Stage 3 nothing to code. The discard is needed rather than merely absent because these records are not silent: a Replication Recipe pre-registration's responses are full of `we will directly replicate Smith (2009)`, so under a whitelist they are admitted by `replication-claim-text` (730) and would buy two voters to be told what the template already said. That is the only reason precedence is 935, above the whole admission band. It sits BELOW `osf-registration-completed` (936) on purpose: the two match blocks are hand-synced complements — the format gives a spec no way to reference another — so if they ever drift the row is kept and screened rather than deleted, the cheaper of the two errors, and `tests/test_osf_registrations.py` asserts the partition. It sits below the definitional discards (940-960): those say the record is not a paper at all, this one says the record is a registration whose own template says no study has been completed. It fires on nothing until a backfill has run — the first line it matches exists only in overlay text this project wrote. Evidence, the 2026-08-04 refused promotion and the 2026-08-05 re-adjudication that promoted it: `filter/spec/rule_ideas.md` §2b and `analysis/osf_registrations/REPORT.md`. DOMAIN: The OSF records this rule speaks for, by either identifier the pool gives them — a DOI on the OSF registrant, or, for a row with no DOI at all, an osf.io URL, which is the same population `osf_identifier()` accepts and the same GUID pattern `osf_registration_guid()` reads. The URL arm is not decoration: 202 of the 367 OSF records in the 2026-08-08 export have no DOI, and a DOI-only domain reported 1,954 matched across the backfill that took the rule from 1,954 to 2,103 matches (releases 78607c53327d → ce0ba03ce326), because every newly matched work sat outside the declared population. Its match reads a line that exists only in overlay text this project wrote, so a registration the backfill gave no text is outside what the rule can reach while staying inside what it claims to govern; the route report counts that gap (see CONVENTIONS.md, \"domain\").",
  "match": {
    "abstract_missing": false,
    "abstract_regex": "^OSF registration template: ",
    "none_of": [
      {
        "any_of": [
          {
            "abstract_regex": "^OSF registration template: [^\\n]*post[-\\s]?completion"
          },
          {
            "all_of": [
              {
                "abstract_regex": "^OSF registration template: [^\\n]*open[-\\s]?ended registration"
              },
              {
                "text_regex": "\\breplicat\\w*"
              }
            ]
          }
        ]
      }
    ]
  },
  "domain": {
    "any_of": [
      {
        "doi_prefix": [
          "10.17605"
        ]
      },
      {
        "all_of": [
          {
            "doi_regex": "^$"
          },
          {
            "url_regex": "osf\\.io/(?:v2/(?:nodes|registrations)/)?[a-z0-9]{5,}"
          }
        ]
      }
    ]
  },
  "pile": "discard",
  "vocabulary": null,
  "precedence": 935,
  "shadow": false,
  "measured": [
    {
      "level": "trusted",
      "date": "2026-08-05",
      "owner": "flora-extractor",
      "rationale": "Promotion to a live discard. Match block unchanged from the refused 2026-08-04 promotion, so its enumeration and recall gate still describe this population. The single blocking case (pr8a4) was re-read whole by the maintainer on 2026-08-05 and ruled prospective — a mock write-up on a pre-analysis plan, not a completed replication — bringing the sample's real-study count to zero. Discard is a routing pile, not a deletion: the works stay in the pool and a future bundle can route them again."
    },
    {
      "level": "llm:claude-sonnet-5",
      "n": 300,
      "sample": "300 rows drawn uniformly at random (random_state=17) from the 1,308 rows this rule discards over the full no-abstract 10.17605 population, 2026-08-04. Read by six claude-sonnet-5 agents over the registration's own recovered text, truncated at 3,000 characters, asked only whether the record reports results of a replication already run, told to judge from the text alone and to err toward keeping. Labels come from the record, nothing downstream of this rule. Artifacts: analysis/osf_registrations/{REPORT.md, review_sample.csv, review_verdicts.csv}.",
      "date": "2026-08-04",
      "owner": "flora-extractor",
      "rationale": "259 plan / 36 unclear / 5 flagged; all five read by hand. One is a real loss: 10.17605/osf.io/pr8a4, a completed replication of Kerner's FDI models written up in past tense on the EGAP template. Two were over-calls stating a PRIOR study's outcome (a class project; the van 't Veer template's prior-work field), one an inference from a journal-acceptance sentence, and one a completed botany study that is not a replication at all and so costs nothing. One real study in 300 of 1,308 is a point estimate of V/n = 4.4 papers lost against a database holding ~1,500, so CONVENTIONS.md's rule applies: do not promote, narrow the rule, start again. The 36 unclear rows push the same way — most have no content beyond `looked: No`, and a rule that deletes what nobody can read is the case the evidence gate exists for."
    },
    {
      "level": "heuristic",
      "n": 3016,
      "sample": "The whole no-abstract population on registrant 10.17605 — 25,819 pool rows on the registrant, 3,016 without an abstract, counted on disk by analysis/osf_registrations/build_worklist.py. Templates fetched for all of them 2026-08-04; 1,674 resolved to registrations, 1,342 are OSF projects/components the registrations endpoint 404s on. Each row put through this spec and its twin by eval_spec_rows().",
      "date": "2026-08-04",
      "owner": "flora-extractor",
      "rationale": "An exact count over a named population, not a reading of what the rule discards — which is why the level is heuristic even though nothing here is estimated. It settles two things and neither is precision. (1) The negation holds across the enumerated vocabulary: thirteen templates, every one outside the keep arms a pre-data-collection form. (2) The recall gate passes exactly: 0 known FLoRA papers among the 1,308 discards, over the population rather than a sample — and 0 under every version of the gold standard measured so far, which is the part worth keeping. The denominator moved three times: 8 known FLoRA rows by a DOI-only match, 16 once OSF records FLoRA names by URL are counted (`_osf_doi_keys()` in shared/flora_skip.py), and 64 against the flora.csv regenerated 2026-08-05, the first release carrying the OSF registration DOIs added to the COS source sheet that day. The numerator stayed at zero throughout. It also corrects a number the 60-row sample produced — the population holds far more than the 10 known FLoRA papers estimated — and settles that no post-data-collection template exists here at all, so dropping that keep arm cost nothing."
    }
  ]
}
