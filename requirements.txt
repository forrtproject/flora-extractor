# FLoRA Extractor — Web Dashboard Dependencies (Railway)
# Minimal set for the read-only monitoring dashboard.
# Full pipeline deps are in requirements-pipeline.txt.

flask>=3.0.0
pandas>=2.0.0
pyarrow>=14.0.0
requests>=2.31.0
python-dotenv>=1.0.0
gunicorn>=21.2.0
