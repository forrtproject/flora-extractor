# FLoRA Extractor — Python Dependencies
# Install with: pip install -r requirements.txt
# On Railway, FLORA_READONLY=1 prevents heavy pipeline deps from being imported.

# ── Web / dashboard (Railway uses these) ──────────────────────────────────────
flask>=3.0.0
gunicorn>=21.2.0
pandas>=2.0.0
pyarrow>=14.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# ── Pipeline only (not used when FLORA_READONLY=1) ────────────────────────────
flask-sqlalchemy>=3.1.0
openpyxl>=3.1.0
fpdf2>=2.7.0
openai>=1.0.0
pdfminer.six>=20221105
pymupdf>=1.23.0
lxml>=4.9.0            # lazily imported by shared/grobid.py and shared/pdf_sources.py
playwright>=1.40.0
rapidfuzz>=3.0.0
pyyaml>=6.0
filelock>=3.12.0
fsspec[http]>=2024.6.0  # lazily imported by search/snapshot_scan.py — HTTP range reads over the OpenAlex parquet bucket
huggingface_hub>=0.34.0  # lazily imported by search/pool_sync.py — shares the snapshot survivor pool via a private HF dataset repo

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-mock>=3.0.0
ruff>=0.15.0
